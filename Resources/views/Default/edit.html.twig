{% extends 'VlRadRestBundle:Default:skeleton.html.twig' %}
{% block radrest_content %}
<h1>Edit</h1>
{% if form is defined %}
  {{ form(form, {action: path(controller.route('put'), {id: id})}) }}
{% endif %}
{% include 'VlRadRestBundle:Default:links.html.twig' with {data: form.vars.value} %}
{% endblock %}
